<div class="container">
  <div class="row">
		<h2>Editing <%= @car.name %></h2>

		<%= render 'form' %>

		<%= link_to 'Show', @car %> |
		<%= link_to 'Back', admin_cars_path %> |
		<%= link_to 'Images', admin_car_car_images_path(@car) %>
	</div>
</div>

